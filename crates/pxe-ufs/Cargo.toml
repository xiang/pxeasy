[package]
name = "pxe-ufs"
version = "0.1.0"
edition = "2021"
authors = ["Gemini CLI"]
description = "Write-only UFS2 filesystem image writer"

[dependencies]
crc = "3.4"
log = "0.4"

[lib]
path = "src/lib.rs"

[dev-dependencies]
tempfile = "3"
